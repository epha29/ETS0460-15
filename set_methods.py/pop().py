my_set={ 10, 20, 30, 40,  50 ,60, 70,}
popped_item=my_set.pop()# remove and return an arbitrary element from the set
print(popped_item)
print(my_set)# we expect the output to be the set without the popped item 
